// components import
import Hero from '@/components/process/hero/Hero'
import Processes from '@/components/process/processes/Processes'

const Process = () => {
  return (
    <main className="process page-container">
        <section className='section-container'>
            <Hero />
            <Processes />
        </section>
    </main>
  )
};

export default Process;
